//! north-persistence: see the crate-level plan in docs/architecture/.
//!
//! Implementation lands with the OpenSpec change `introduce-email-auth-and-owner-bootstrap`.
//! Deliberately dependency-free until then so the workspace builds fast and
//! architecture boundaries stay trivially auditable.
